package com.example.bottomlinelocations.database

import android.content.Context
import android.database.sqlite.SQLiteDatabase
import android.database.sqlite.SQLiteOpenHelper
/*
private const val DB_NAME = "BottomlineLocations"
private const val DB_VERSION = 1
private const val TABLE_NAME = "siteDefects"

class MySqlLiteOpenHelper(context: Context) : SQLiteOpenHelper(
    context,
    DB_NAME,
    null,
    DB_VERSION
) {
    override fun onCreate(db: SQLiteDatabase?) {
        db!!.apply{
            //delete table
            execSQL("DROP TABLE IF EXISTS $TABLE_NAME")

            // Create table
            //execSQL("CREATE TABLE $TABLE_NAME
                //SiteDefects(1, "Esso Brabantbad", "Kerkstraat 3", "","Telemetry of site is defect, this needs to be repaired"  ,"2020-10-03 1200"),



            //
            //execSQL()

        }
    }

}

 */