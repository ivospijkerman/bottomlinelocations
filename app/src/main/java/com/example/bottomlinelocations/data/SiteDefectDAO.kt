package com.example.bottomlinelocations.data
/*
import androidx.room.Dao
import androidx.room.Insert
import androidx.room.Query

@Dao
interface SiteDefectDAO {

    @Query("SELECT * FROM sitedefect ORDER BY id")
    suspend fun getAll(): List<SiteDefects>

    @Query("SELECT * FROM sitedefect WHERE id = :id LIMIT 1")
    suspend fun getById(id: Int): SiteDefects?

    @Insert
    suspend fun insert(siteDefects: SiteDefects)

    @Query("DELETE FROM sitedefect")
    suspend fun deleteAll()
}



 */